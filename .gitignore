# Python
__pycache__/
*.pyc

# Logs / scratch
*.log
_*.txt

# Generated / large data — regenerate via scrapers/ (see README)
data/pdf/
data/cycorg_samples/
data/raw/bravelog_by_contest/
data/raw/bravelog_contests_*.json
# raw iRunner pulls contain unmasked names (PII) — never commit
data/raw/irunner_*
data/raw/*.html
data/processed/*.json
!data/processed/*summary*.json
data/processed/_*

# Web (Phase 2)
web/node_modules/
web/dist/
web/.astro/
# NOTE: web/public/data/ IS committed (deploy artifact) — Vercel build has no Python
# to regenerate it. Refresh with: python scrapers/build_viz.py

# OS
.DS_Store
Thumbs.db

# font subsetting source TTFs (woff2 outputs in web/public/fonts ARE committed)
data/_fonts_src/

.playwright-mcp/

# agent/session scratch
.claude/
.superpowers/

# open-dataset release assets (regenerated by scrapers/build_dataset.py)
data/dist/
